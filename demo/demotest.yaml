apiVersion: camel.apache.org/v1
kind: Integration
metadata:
  name: demotest
spec:
  flows:
    - route:
        from:
          uri: timer:test
          steps:
            - log:
                message: test
          parameters:
            period: '2000'
